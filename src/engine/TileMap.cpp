#include "TileMap.hpp"

TileMap::TileMap() {

}

void TileMap::init() {
//	m_chunks.push_back(std::make_shared<TileMapChunk>(0, 0, 10, 10, this));
//	m_chunks.push_back(std::make_shared<TileMapChunk>(10, 10, 10, 10, this));
}

void TileMap::update(float delta) {
//	for (auto chunk : m_chunks) {
//		chunk->update(delta);
//	}       
}
